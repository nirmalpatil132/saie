# Endpoint the contact form POSTs { name, email, message } to as JSON.
# Works well with a form backend such as Formspree, Getform, or a simple
# serverless function. Leave unset to keep the form UI visible with a
# clear "not configured" state instead of failing silently.
VITE_FORM_ENDPOINT=
